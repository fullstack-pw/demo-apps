apiVersion: apps/v1
kind: Deployment
metadata:
  name: trace-demo
spec:
  replicas: 1
  template:
    spec:
      containers:
        - name: trace-demo
          env:
            - name: ENV
              value: "stg"
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: trace-demo
  annotations:
    external-dns.alpha.kubernetes.io/hostname: "stg.trace-demo.fullstack.pw"
spec:
  tls:
    - hosts:
        - "stg.trace-demo.fullstack.pw"
      secretName: trace-demo-tls
  rules:
    - host: stg.trace-demo.fullstack.pw
